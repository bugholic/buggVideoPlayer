name: Build EAS App

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Setup EAS CLI
      uses: expo/expo-github-action@v8
      with:
        eas-version: latest
        token: ${{ secrets.EAS_TOKEN_LOGIN }}

    - name: Build Android
      run: eas build --platform android --local --profile development
      env:
        EAS_TOKEN_LOGIN: ${{ secrets.EAS_TOKEN_LOGIN }}

    - name: Upload Android APK
      uses: actions/upload-artifact@v4
      with:
        name: android-app
        path: |
          *.apk
          *.aab
        retention-days: 30
